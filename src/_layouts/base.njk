<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/styles/index.css">
    <title>{{ title or "Fake Hemingway" }}</title>
</head>
<body class="font-sans bg-slate-50 text-slate-900">
    {% if title == "Fake Hemingway" %}
        <!-- Writing Tool Layout -->
        <header class="bg-white border-b border-slate-200 h-[70px] sticky top-0 z-50 flex items-center justify-between px-6 backdrop-blur shadow-sm">
            <div class="flex items-center gap-6">
                <h1 class="text-xl font-semibold text-slate-900 m-0">
                    <a href="/" class="no-underline text-inherit">Fake Hemingway</a>
                </h1>
                <div class="flex items-center gap-3">
                    <label for="writing-mode-select" class="text-sm font-medium text-slate-700">Writing Mode:</label>
                    <select name="writingMode" id="writing-mode-select" class="bg-slate-100 border border-slate-300 rounded-lg px-3 py-2 text-sm font-medium text-slate-900 cursor-pointer transition-all hover:bg-white hover:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500/30 min-w-[180px]">
                        <option value="standard">Standard Analysis</option>
                        <option value="brevity">Brevity Mode</option>
                        <option value="conversational">Conversational Mode</option>
                        <option value="marketing">Marketing Copy Mode</option>
                    </select>
                </div>
            </div>
            <div class="flex items-center gap-3">
                <button class="flex items-center gap-2 bg-transparent border border-slate-300 rounded-lg px-3 py-2 text-sm font-medium text-slate-600 cursor-pointer transition-all hover:bg-slate-100 hover:border-blue-500 hover:text-slate-900 relative" id="settings-btn" onclick="toggleSettings()" title="Settings">
                    <span class="text-base">⚙️</span>
                </button>
                
                <!-- Settings Dropdown -->
                <div id="settings-dropdown" class="absolute top-[calc(100%+8px)] right-0 bg-white border border-slate-200 rounded-xl p-5 shadow-lg min-w-[320px] z-50 opacity-0 invisible -translate-y-2 transition-all">
                    <h3 class="text-base font-semibold mb-4 text-slate-900">Analysis Settings</h3>
                    <div class="mb-5">
                        <h4 class="text-sm font-semibold mb-3 text-slate-600">Passive Voice Detection</h4>
                        <div class="space-y-2">
                            <label class="flex items-start gap-3 cursor-pointer">
                                <input type="radio" name="passiveDetection" value="enhanced" checked class="mt-1">
                                <div>
                                    <div class="font-medium text-slate-900">Enhanced Detection</div>
                                    <small class="text-slate-600">Detects irregular verbs, handles adverbs, more accurate</small>
                                </div>
                            </label>
                            <label class="flex items-start gap-3 cursor-pointer">
                                <input type="radio" name="passiveDetection" value="original">
                                <div>
                                    <div class="font-medium text-slate-900">Basic Detection</div>
                                    <small class="text-slate-600">Basic detection for -ed endings only</small>
                                </div>
                            </label>
                        </div>
                    </div>
                </div>
            </div>
        </header>
        <main>
            {{ content | safe }}
        </main>
    {% else %}
        <!-- Blog/Article Layout -->
        <header class="bg-white border-b border-slate-200 py-4 sticky top-0 z-50 backdrop-blur shadow-sm">
            <div class="max-w-6xl mx-0 auto px-8 flex items-center justify-between h-[70px]">
                <h1 class="text-xl font-semibold m-0">
                    <a href="/" class="no-underline text-slate-900">Fake Hemingway</a>
                </h1>
                <nav class="flex items-center gap-8">
                    <a href="/" class="no-underline text-slate-600 font-medium transition-colors hover:text-blue-600">Writing Tool</a>
                </nav>
            </div>
        </header>
        <main class="max-w-6xl mx-auto px-8">
            {{ content | safe }}
        </main>
    {% endif %}
    {% include "footer.njk" %}
</body>
</html>
